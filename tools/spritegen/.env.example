# Copy to .env and fill in. .env is gitignored.

# Required: your OpenAI API key.
OPENAI_API_KEY=sk-...

# Optional: override the vision-QA critique model (used only with --vision-qa).
# SPRITEGEN_VISION_MODEL=gpt-5.1
